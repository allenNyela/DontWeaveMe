using UnityEngine;

public class GameManagerCaller : MonoBehaviour
{
    public bool hasEndTurn = false;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void EndTurn()
    {
        //if (hasEndTurn)
        //    return;
        //bool hasEnd = GameManager.Instance.EndTurn();
        //if (hasEnd)
        //{
        //    hasEndTurn = true;
        //}

    }
}
